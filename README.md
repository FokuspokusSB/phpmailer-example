# PHPMAILER Example

Config in `mail.config.php`.

```php
<?php
$CONFIG = array(
  'mail' => array(
    "host" => "",
    "port" => "587",
    "email" => "",
    "password" => "",
    "isSMTP" => true,
    "debug" => true
  )
);
```


## test
```sh
$ php -S localhost:8000
```